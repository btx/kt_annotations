var anno = {};

/**
 * @param {Object} annotation the annotation
 * @param {Object=} opt_replace optionally, an existing annotation to replace
 */
anno.addAnnotation =  function (annotation, opt_replace) {

};

anno.makeAnnotatable =  function (item) {

};
